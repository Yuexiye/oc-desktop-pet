# oc-pet 最小依赖（核心功能）
# 安装: pip install -r requirements-minimal.txt
#
# 最小依赖集：桌宠能跑起来 + 基础对话 + Live2D 渲染
# 完整功能请安装 requirements.txt

# ── L1 核心依赖（必须）──
PySide6~=6.5          # 6.5.x，不升 6.6
PySide6-Addons~=6.5
requests~=2.28
Pillow~=9.0
PyYAML~=6.0
websocket-client~=1.8

# ── L2 Live2D 渲染 ──
live2d-py~=1.0

# ── L3 TTS 依赖（语音合成）──
edge-tts~=6.1         # 微软 Edge TTS（免费在线，默认引擎）

# ── 其他依赖 ──
pydantic~=2.0