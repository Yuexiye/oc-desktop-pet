# Handoff — 2026-09-03 T10 D3-cheap

## 本次交付

| 任务 | 内容 | 状态 |
|------|------|------|
| T10 | D3-cheap：intensity 透传 + V/A 中间表示 | ✅ 完成 |

## 改动文件

- `avatar/live2d_renderer.py`（唯一改动文件）

## 实现细节

### 1. V/A 情感空间（`:126-165`）

```python
_EMOTION_VA = {
    "neutral":   (0.0,  0.0),  # 中性
    "happy":     (0.8,  0.7),  # 积极+兴奋
    "cute":      (0.7,  0.5),  # 积极+中等兴奋
    "surprised": (0.3,  0.9),  # 轻度积极+高兴奋
    "thinking":  (0.2,  0.2),  # 轻度积极+低兴奋
    "sad":       (-0.7, -0.3), # 消极+平静
    "angry":     (-0.6,  0.8), # 消极+高兴奋
}
```

`_va_interpolate_targets(va_cur)`：在 V/A 空间找最近两个情绪，bilinear 插值 12 参数目标值。

### 2. intensity 透传（`:283-286`, `:1252-1262`, `:2177-2178`）

- `set_emotion(emotion, intensity)` → 存储 V/A 目标 + 强度
- `_update_procedural_emotion()` → 从 V/A 插值 targets → 按 intensity 缩放（breath_amp/rate 除外）
- `set_master_emotion(emotion)` → 同步 V/A 目标（供 pet.py 高频调用）

### 3. 平滑过渡（`:1239-1250`）

V/A 坐标用帧率无关指数平滑过渡：
```python
alpha = 1.0 - math.exp(-dt / tau)
va_cur = (va_cur[0] + (va_target[0] - va_cur[0]) * alpha,
          va_cur[1] + (va_target[1] - va_cur[1]) * alpha)
```

## 测试结果

```
435 passed in 45.92s
```

## 后续注意

- `working` / `missing` 两档情绪无 V/A 坐标，回退到 neutral（与原有行为一致）
- intensity 默认 1.0，不改变原有行为
- V/A 插值是 bilinear（最近两点），非 Delaunay 三角化——简单够用
