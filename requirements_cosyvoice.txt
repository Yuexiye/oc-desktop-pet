# CosyVoice2 本地 TTS 运行依赖（基于已验证可跑出 8-10s 延迟的环境）
#
# 注意：torch / torchaudio 不在这里装——PyPI 上的 torch 不带 CUDA，
# 必须由 scripts/setup_tts_env.py 从 pytorch 的 cu12x wheel 源单独安装，
# 否则本地 TTS 会跑在 CPU 上（每句 60-150s）。
#
# 其余包从 PyPI 安装。版本均来自验证过的环境，可复现该延迟表现。

# ── ONNX 解码器 / cuDNN（GPU 合成的关键）──
onnxruntime-gpu==1.20.2
nvidia-cudnn-cu12==9.24.0.43
onnx==1.16.0

# ── CosyVoice / FunASR 推理栈 ──
funasr==1.3.1
modelscope==1.20.0
transformers==4.51.3
tokenizers==0.21.4
sentencepiece==0.2.1
diffusers==0.29.0
wetext==0.0.4
HyperPyYAML==1.2.3
torch-complex==0.4.4
torchmetrics==1.9.0
accelerate==1.13.0
safetensors==0.7.0
torchvision==0.26.0

# ── 音频 / 数值基础 ──
librosa==0.10.2
soundfile==0.12.1
scipy==1.17.1
numpy==1.26.4

# ── 通用 ──
PyYAML==6.0.3
tqdm==4.67.3
regex==2026.4.4
requests==2.34.2
filelock==3.29.0
fsspec==2024.12.0
